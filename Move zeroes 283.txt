class Solution:
    def moveZeroes(self, nums: List[int]) -> None:

        n=len(nums)
        p=0
        for i in range(n):
            if nums[i]!=0:
                nums[p]=nums[i]
                p+=1
        for i in range(p,n):
            nums[i]=0
